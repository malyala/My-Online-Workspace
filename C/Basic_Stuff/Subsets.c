#include <stdio.h>
/*
All subsets of a generic array.

Currently incomplete.
*/
void **Subsets(void *arr, int len, size_t size){
	if (len  < 1)
		
		return ;
	else{
		char first = str[0];
		

	}
}




